<section{{ wrapper_attributes }}>
  {%- if title is not empty -%}
    <h3>{{ title }}</h3>
  {%- endif -%}

  <{{ list_type }}{{ attributes }}>
    {%- for item in items -%}
      <li{{ item.attributes }}>{{ item.value }}</li>
    {%- endfor -%}
  </{{ list_type }}>
</section>
